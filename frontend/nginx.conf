server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback：未知路徑一律回傳 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
