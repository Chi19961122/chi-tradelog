# Docker Compose 環境變數範例。
# 複製為 .env（同目錄）後填入實際值；.env 不進版控。
# 未設定時 docker-compose.yml 會使用開發預設值（僅限本機開發，不可用於正式環境）。

# PostgreSQL 密碼（postgres 使用者）
POSTGRES_PASSWORD=postgres

# JWT 簽章金鑰（HS256，至少 32 bytes；正式環境務必更換為隨機長字串）
JWT_KEY=dev-only-signing-key-change-in-production-min-32-bytes-long!!
